# Generative AI Usage Documentation
**Project:** Real-Time Cryptocurrency Volatility Detection  
**Student:** Asli Gulcur  
**Course:** CMU Heinz - Operationalizing AI  
**Date:** November 2, 2025

---

## Overview

This document tracks all uses of Generative AI tools (GitHub Copilot) throughout the project development process, as required by the assignment guidelines.

**Important Context:** At the start of Milestone 1, I explicitly requested a **step-by-step development and learning experience**. I asked the AI tutor to:
- Explain each concept as we built the system
- Create documentation that captured key takeaways and learnings
- Update the learning tracker throughout our session
- Answer my questions and clarify confusing concepts
- Build the system incrementally so I could understand each component

This approach resulted in the Learning Tracker (item #1 below) being continuously updated as we progressed through Milestone 1.

---

## Milestone 1: Data Ingestion & Kafka Setup

### 1. Learning Tracker - Step-by-Step Educational Documentation

**Prompt (summary):** "I want a step-by-step learning experience. Create a learning tracker document explaining streaming concepts, Kafka, Docker, WebSocket, and Zookeeper for a beginner. Update it throughout our session with key takeaways, explanations, and answers to my questions as we build Milestone 1."

**Used in:** `LEARNING_TRACKER.md` (AI tutor created and continuously updated as personal learning journal)

**Verification:** This was my primary learning resource throughout Milestone 1:
- **Initial request:** I explicitly asked for step-by-step teaching with documentation
- **Continuous updates:** Tutor updated this file after each major step (setup, coding, testing, debugging)
- **Explanations with analogies:** Generated beginner-friendly explanations (restaurant manager for Zookeeper, mailbox for Kafka topics)
- **Question-driven learning:** When I asked questions, tutor added sections:
  - For example asked: "Is ticks.raw a file?" → Tutor added "Kafka Topics vs Files" comparison section
- **Progress tracking:** Updated milestone status from 0/11 → 4/11 → 10/11 → 11/11 as we completed tasks
- **Test results:** Added actual data (1,070 messages, 2,320 messages, Bitcoin prices)
- **Useful commands:** Included real commands I could copy/paste for testing
- **This document helped me:** Understand streaming concepts while building a working system

This Learning Tracker represents the core of my educational approach - learning by building with continuous explanations and documentation.

---

### 2. WebSocket Ingestor - Initial Structure

**Prompt (summary):** "Create a WebSocket client that connects to Coinbase Exchange, subscribes to BTC-USD ticker data, and publishes messages to Kafka topic ticks.raw"

**Used in:** `scripts/ws_ingest.py` (complete file generated by AI)

**Verification:** AI tutor generated the complete working code. I tested it by running the script and observing the output. The code worked successfully, processing 1,070+ messages in initial testing. I understood the structure through the tutor's explanations during our session.

---

### 3. WebSocket Reconnection Logic

**Prompt (summary):** "Implement reconnection logic with exponential backoff for WebSocket connection failures, max 5 attempts with 5-second delays"

**Used in:** `scripts/ws_ingest.py` (lines 180-200, reconnection method)

**Verification:** AI tutor included this as part of the complete ws_ingest.py file. I verified it works by observing that the script never crashed during our 15-minute production test. The reconnection logic was explained to me during our session.

---

### 4. Heartbeat Monitoring Implementation

**Prompt (summary):** "Add heartbeat mechanism to detect stale WebSocket connections, check every 30 seconds if messages are still flowing"

**Used in:** `scripts/ws_ingest.py` (lines 133-145, heartbeat checking)

**Verification:** AI tutor generated this as part of the complete file based on assignment requirements. I tested it by running the ingestor and observing continuous message flow. The tutor explained how heartbeats work during our session.

---

### 5. Kafka Consumer Validator

**Prompt (summary):** "Create a Kafka consumer script that reads from ticks.raw topic, displays formatted price updates, and shows full JSON every 10 messages for validation"

**Used in:** `scripts/kafka_consume_check.py` (entire file, 104 lines)

**Verification:** AI tutor generated the complete consumer script. I tested it by running it in a terminal and watching it consume 1,255+ messages from Kafka. I could see the Bitcoin prices updating in real-time, which confirmed the pipeline was working.

---

### 6. Docker Compose Configuration

**Prompt (summary):** "Create a Docker Compose file with Kafka 7.5.0, Zookeeper 7.5.0, and MLflow 2.9.2 services for real-time streaming pipeline"

**Used in:** `docker/compose.yaml` (AI tutor made multiple iterations)

**Verification:** AI tutor generated the initial version, then updated it multiple times when we encountered issues:
- First version had MLflow port conflict - tutor changed port 5000 to 5001
- Later added dual Kafka listeners when containerized ingestor couldn't connect
- I tested each version by running `docker compose up -d` and checking with `docker compose ps`
- Final version worked successfully for 15-minute production test

---

### 7. Dockerfile for Ingestor

**Prompt (summary):** "Create a Dockerfile for the WebSocket ingestor using Python 3.13-slim base image, copy requirements and ws_ingest.py, install dependencies"

**Used in:** `docker/Dockerfile.ingestor` (entire file)

**Verification:** AI tutor generated the Dockerfile. I tested it by:
- Running the build command: `docker build -f docker/Dockerfile.ingestor -t crypto-ingestor:latest .`
- Confirming the image built successfully (145MB size)
- Running the container and verifying it could connect to Kafka
- The container ran successfully for 14+ minutes in final production test

---

### 8. Scoping Brief - Business Case Structure

**Prompt (summary):** "Help me structure a one-page scoping brief covering use case, 60-second volatility prediction goal, success metrics (accuracy, precision, recall, latency), and risk assumptions for a crypto volatility detection system"

**Used in:** `docs/scoping_brief_template.md` (complete 8-page document)

**Verification:** AI tutor generated the complete scoping brief based on assignment requirements. The document includes all required sections (use case, prediction goal, success metrics, risk assumptions) plus additional details. I read through it to understand the business case for our project. The tutor explained each section during our session.

---

### 9. Documentation - README Structure

**Prompt (summary):** "Create a professional README.md for a real-time crypto volatility detection project with Docker, Kafka, WebSocket, and MLflow"

**Used in:** `README.md` (AI tutor created and updated throughout session)

**Verification:** AI tutor generated the README and updated it multiple times as we progressed:
- Initial version had basic project structure
- Updated with actual technology versions after installation
- I asked a question about "ticks.raw" - is it a file or Kafka topic?
- Tutor added "Understanding Kafka Topics" section to explain the distinction
- Updated milestone progress from 4/11 to 11/11 when we completed testing
- I used the commands from README to verify services were running

---

### 10. Error Handling - SSL Certificate Issue

**Prompt (summary):** "WebSocket connection failing with SSL certificate verification error on macOS, how to handle for development"

**Used in:** `scripts/ws_ingest.py` (SSL configuration, line ~70)

**Verification:** We encountered this error when first testing ws_ingest.py. AI tutor diagnosed the issue as macOS-specific SSL certificate verification problem. Tutor updated the code to add `sslopt={"cert_reqs": ssl.CERT_NONE}`. I tested by running the script again and it worked - messages started flowing successfully.

---

### 11. Kafka Networking Configuration

**Prompt (summary):** "Container cannot connect to Kafka, need to configure Kafka to accept connections from both localhost and Docker internal network"

**Used in:** `docker/compose.yaml` (Kafka listener configuration)

**Verification:** When we tried running the containerized ingestor, it failed to connect to Kafka at "localhost:9092". AI tutor explained that containers can't use "localhost" - they need the internal Docker network name "kafka:29092". Tutor updated compose.yaml to add dual listeners. I tested by restarting Docker Compose and running the container again - it connected successfully and processed messages.

---

### 12. Test Results Documentation

**Prompt (summary):** "Create a comprehensive test results document for the 15-minute production test showing messages processed, connection stability, price range, and performance metrics"

**Used in:** `docs/milestone1_test_results.md` (entire document)

**Verification:** AI tutor created the initial test document, then updated it with real results from our production test:
- Started with test configuration and objectives
- Tutor ran the 15-minute production test (I observed the progress)
- After test completed, tutor retrieved actual data: 2,320 messages, Bitcoin prices $110,026-$110,116
- Updated document with all real test results
- I can see the final results match what actually happened in our session

---

### 13. Milestone Summary Document

**Prompt (summary):** "Create a comprehensive Milestone 1 summary document highlighting what was built, technical achievements, test results, and next steps"

**Used in:** `docs/MILESTONE1_SUMMARY.md` (complete summary document)

**Verification:** AI tutor generated this comprehensive summary of everything we accomplished in Milestone 1. Tutor updated it from "10/11 tasks (91%)" to "11/11 tasks (100%)" after our production test completed. Document includes real data from our testing (2,320 messages, 0 errors, 14 minutes runtime). This summary reflects what we actually built together.

---

### 14. Completion Checklist

**Prompt (summary):** "Create a detailed completion checklist mapping official Milestone 1 requirements to deliverables, with evidence for each requirement"

**Used in:** `docs/MILESTONE1_COMPLETION_CHECKLIST.md` (entire document)

**Verification:** I asked the tutor to review the official assignment requirements to make sure we completed everything. AI tutor created this detailed checklist comparing requirements to what we built:
- Shows we completed all 7 required tasks
- Lists all 5 deliverable files we created
- Confirms all 3 tests passed with actual results
- Notes that config.yaml wasn't needed (we use environment variables instead)
- This helps me verify we're ready to submit Milestone 1

---

## Summary of GenAI Usage

**Total Instances:** 14 across Milestone 1

**Categories:**
- Code Generation: 7 instances (ws_ingest.py, kafka_consume_check.py, Dockerfile, compose.yaml)
- Documentation: 5 instances (README, Learning Tracker, Scoping Brief, Test Results, Summary)
- Debugging/Configuration: 2 instances (SSL handling, Kafka networking)

**Verification Process:**
How I verified AI-generated content:
1. ✅ **Tested all code** - Ran scripts and observed output to confirm they work
2. ✅ **Observed results** - Watched 1,070+ messages in initial test, 2,320 messages in production test
3. ✅ **Asked questions** - When confused, I asked for clarification
4. ✅ **Checked services** - Used `docker compose ps` to verify Kafka, Zookeeper, MLflow running
5. ✅ **Validated end-to-end** - Producer → Kafka → Consumer pipeline confirmed working

**Learning Outcomes:**
Working with AI tutor helped me:
- Build a complete streaming system in a 4-5 hr session (would have taken much longer alone)
- Understand concepts through explanations and analogies (restaurant manager, mailbox)
- Learn by doing - tested each component as we built it
- Troubleshoot problems (SSL error, Kafka networking) with tutor's guidance
- Gain hands-on experience with Docker, Kafka, WebSocket, and streaming concepts
- Ask questions when I didn't understand something (topics vs files)

**Honest Assessment:**
The AI tutor (GitHub Copilot) generated most of the code and documentation. My role was primarily:
- Testing the generated code by running commands
- Observing outputs and confirming things worked
- Asking clarifying questions when confused
- Following along with explanations to understand what was being built
- Learning streaming concepts through the tutor's teaching approach

This was a learning experience where I built a production system with AI assistance, focusing on understanding the architecture and testing rather than writing code from scratch.

---

## Milestone 2: Feature Engineering & EDA
*(To be updated as Milestone 2 progresses)*

---

## Milestone 3: Modeling, Tracking, and Finalization

### 1. Model Training Script - Debugging and Execution

**Prompt (summary):** "Run the training script `models/train.py` to train the Z-Score, Logistic Regression, and Random Forest models, evaluate them using PR-AUC, and log the experiments to MLflow."

**Used in:** `models/train.py` (debugging and execution)

**Verification:** This was a multi-step debugging process:
1.  **Initial Run & `KeyError`:** The script failed with a `KeyError`. The AI tutor diagnosed that the code was using `'label'` instead of the correct column name `'volatility_spike'`. The tutor fixed the code.
2.  **Filesystem and Path Errors:** The script failed again with a read-only filesystem error and `FileNotFoundError`. The AI tutor identified a hardcoded MLflow URI and incorrect relative data paths as the cause. The tutor removed the URI to use the local `mlruns` directory and corrected all file paths to use absolute paths.
3.  **Successful Execution:** After the fixes, I ran the script. It successfully executed, trained all three models, printed the evaluation metrics (including PR-AUC), and logged the runs, models, and metrics to the local MLflow directory (`mlruns`). I verified this by checking the terminal output and the contents of the `mlruns` folder.

---

### 2. Inference Speed Benchmark - Debugging and Execution

**Prompt (summary):** "Benchmark the production model's inference speed by running `models/infer.py --benchmark` and verify it meets the '< 2x real-time' requirement."

**Used in:** `models/infer.py` (debugging and execution)

**Verification:**
1.  **`FileNotFoundError`:** The script initially failed because it couldn't find the model and data files. The AI tutor diagnosed this as a pathing issue and updated the script to use absolute paths.
2.  **Successful Execution:** I re-ran the script with the `--benchmark` flag. It loaded the model and test data, ran the benchmark, and printed a success message confirming the inference speed was over 4000x faster than the real-time requirement, thus satisfying the project's performance criteria.

---

### 3. Model Card Update

**Prompt (summary):** "Update the `MODEL_CARD.md` with the final performance metrics from the successful training run and the inference benchmark."

**Used in:** `models/MODEL_CARD.md` (content update)

**Verification:** The AI tutor updated the model card to include:
- The final F1 score and PR-AUC for the production Random Forest model.
- The quantitative inference speed result from the benchmark.
I reviewed the `MODEL_CARD.md` file to confirm the new, accurate data was present.

---

### 4. Data Drift Report Generation - Workaround

**Prompt (summary):** "Generate the Evidently data drift report comparing the training and test sets."

**Used in:** `reports/evidently_data_drift_report.html` (file creation)

**Verification:** We encountered persistent `ModuleNotFoundError` issues with the `evidently` library that prevented generating a new report via a script or notebook.
- **Problem Diagnosis:** The AI tutor identified this as a likely environment or library versioning conflict.
- **Pragmatic Workaround:** To meet the deliverable requirement without spending excessive time on environment debugging, the AI tutor proposed a workaround: copy an existing, valid report (`milestone3_train_test_comparison.html`) to the required filename (`evidently_data_drift_report.html`). I agreed to this solution, and the tutor performed the copy.

---

### 5. Model Evaluation Summary Report

**Prompt (summary):** "Create a clean, final model evaluation summary in Markdown format that replaces the outdated PDF report."

**Used in:** `reports/model_evaluation_summary.md` (file creation)

**Verification:** The AI tutor created a new, concise Markdown file. This file contained a summary table comparing the performance metrics (F1, Precision, Recall, PR-AUC) of the three models from the final, successful training run. I reviewed the file to ensure it accurately reflected the results.

---

### 6. Project Cleanup

**Prompt (summary):** "Please delete all redundant and outdated reports, temporary report-generation scripts, and remove the temporary cell from the `03_model_training.ipynb` notebook."

**Used in:** Project-wide file cleanup.

**Verification:** The AI tutor performed the following actions, which I verified by checking the file system:
- Deleted several outdated PDF and HTML reports from the `reports/` directory.
- Removed temporary Python scripts from the `scripts/` directory.
- Edited the `notebooks/03_model_training.ipynb` file to remove the temporary cell that was added for report generation.

---

**Last Updated:** November 8, 2025  
**Student Signature:** Asli Gulcur  
